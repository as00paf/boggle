'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _config = require('./config');

var _config2 = _interopRequireDefault(_config);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = {
  /**
  * Determines the size of the matrix
  * based on the number of characters passed in
  * A 0 (zero) is returned for an invalid size
  * eg. boggleSize(9) // 3
  * eg. boggleSize(10) // 0
  * @returns Number
  */
  boggleSize: function boggleSize(size) {
    var counter = _config2.default.minSize;

    while (counter < size) {
      var matrixSize = counter * counter;

      if (matrixSize === size) {
        return counter;
      }

      counter += 1;
    }

    return 0;
  },


  /**
  * Checks if the target exists in source
  * Both are 2-d arrays e.g. arrayMatch([[0, 0]], [0, 0]); => true
  * @returns Boolean
  */
  arrayMatch: function arrayMatch(source, target) {
    return source.some(function (item) {
      return item.every(function (x, index) {
        return x === target[index];
      });
    });
  },
  getBoggleMatrix: function getBoggleMatrix(size, letters) {
    var matrix = [];
    var counter = 0;

    [].reduce.call(letters, function (acc, letter, index) {
      counter += 1;
      acc.push(letter);

      if (counter === size) {
        matrix.push(acc);
        acc = [];
        counter = 0;
      }

      return acc;
    }, []);

    return matrix;
  }
};
module.exports = exports['default'];